import React, { useState } from 'react';
import Router from './Router';


// TODO: Import context API for data persistence
const App: React.FC = () => {
  return (
    <Router />
  );
}

export default App;
